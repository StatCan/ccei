# Schema for the theme setting configuration file of the CCEI theme.

ccei_bootstrap.settings:
  type: theme_settings
  label: 'CCEI settings'
  mapping:
    cdn_provider:
      type: string
      label: 'CDN Provider'
    breadcrumb_home:
      type: integer
      label: 'Breadcrumb Home'
